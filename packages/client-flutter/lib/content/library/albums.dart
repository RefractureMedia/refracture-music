import 'package:flutter/material.dart';

class ContentAlbums extends StatelessWidget {
  const ContentAlbums() : super();

  @override
  Widget build(BuildContext context) {
    return const Text("Albums list");
  }
}