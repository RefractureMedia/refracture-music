import 'package:flutter/material.dart';

class ContentArtists extends StatelessWidget {
  const ContentArtists() : super();

  @override
  Widget build(BuildContext context) {
    return const Text("Artists list");
  }
}